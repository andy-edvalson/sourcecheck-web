# Python 3.11 required (negspacy compatibility)
# Install with: pip install -r requirements.txt

# SourceCheck library from local directory (editable install)
-e ../sourcecheck-py

# FastAPI and server
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Pydantic for validation
pydantic==2.5.0
pydantic-settings==2.1.0
